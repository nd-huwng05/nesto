/** Shared surface styling — matches Branch tab (rounded-2xl / 16px) */
export const UI = {
    screenBg: '#f3f4f6',
    cardBg: '#ffffff',
    cardBorder: '#f3f4f6',
    cardRadius: 16,
    cardPadding: 16,
    sectionGap: 16,
};

export const cardStyle = {
    backgroundColor: UI.cardBg,
    borderRadius: UI.cardRadius,
    borderWidth: 1,
    borderColor: UI.cardBorder,
    padding: UI.cardPadding,
    overflow: 'hidden',
};
